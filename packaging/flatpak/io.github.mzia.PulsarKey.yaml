app-id: io.github.mzia.PulsarKey
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: pulsarkey

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=session-bus
  - --device=all
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=com.system76.CosmicStatusNotifierWatcher
  - --talk-name=org.freedesktop.Notifications
  - --system-talk-name=org.freedesktop.login1
  - --talk-name=org.freedesktop.Flatpak

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/pulsarkey/cargo

modules:
  - name: pulsarkey
    buildsystem: simple
    build-commands:
      - cargo --offline build --release --verbose || cargo build --release --verbose
      - install -Dm755 target/release/pulsarkey /app/bin/pulsarkey
      - ln -sf pulsarkey /app/bin/cosmic-fido2
      - install -Dm644 packaging/flatpak/io.github.mzia.PulsarKey.desktop /app/share/applications/io.github.mzia.PulsarKey.desktop
      - install -Dm644 packaging/flatpak/io.github.mzia.PulsarKey.metainfo.xml /app/share/metainfo/io.github.mzia.PulsarKey.metainfo.xml
      - install -Dm644 packaging/cosmic-fido2.svg /app/share/icons/hicolor/scalable/apps/io.github.mzia.PulsarKey.svg
    sources:
      - type: dir
        path: ../..
