#!/bin/sh
set -e

case "$1" in
    remove|purge)
        # Attempt to clean up pam config if binary exists
        if [ -x /usr/bin/cosmic-fido2 ]; then
            /usr/bin/cosmic-fido2 uninstall || true
        fi
    ;;
esac

exit 0
